{
  "mcpServers": {
    "github": {
      "type": "stdio",
      "command": "python3",
      "args": [
        "scripts/mcp-idle-wrapper.py",
        "npx",
        "-y",
        "@modelcontextprotocol/server-github"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${env:GITHUB_PERSONAL_ACCESS_TOKEN}",
        "MCP_IDLE_TIMEOUT": "1800"
      }
    }
  }
}
